import React from "react";
import FocusOverlay from "../../components/focus/FocusOverlay";

/**
 * FocusWorkspacePage - Standalone page wrapper mapped to the /focus route.
 * Renders the fully customized FocusOverlay canvas.
 */
export const FocusWorkspacePage = () => {
  return <FocusOverlay />;
};

export default FocusWorkspacePage;
